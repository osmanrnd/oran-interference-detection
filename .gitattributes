# Normalize line endings: store text as LF in the repo (avoids CRLF churn on Windows).
* text=auto eol=lf

# Treat these as binary (no line-ending or diff munging).
*.png  binary
*.jpg  binary
*.jpeg binary
*.pdf  binary
*.ipynb -text
